
import React from 'react';
import InteractiveCube from './InteractiveCube';

// We'll simply reuse the InteractiveCube component since it has the same functionality
const RotatingCube = () => {
  return <InteractiveCube />;
};

export default RotatingCube;
